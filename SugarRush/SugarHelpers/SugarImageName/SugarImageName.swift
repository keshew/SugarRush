public enum SugarImageName: String {
    case mainBackground,gameBackground
    case candyForeground, menuForegroundImage, playForeground, recordsForegroundImage, shopForegroundImage, gearForeground
    case wideButtonBackground, squareButtonBackground, purpleLabelBackground
    case backArrow
    case done
    case minus, plus
    case closedLevel
    case backArrow2, nextArrow
    case hummer, time
    case countBonusBackground
    case lines
    case blockToBottom, blockToLeft, blockToRight, blockToTop
    case twiter, facebook, apple
}
